
mysql_config = {
    'host': 'your-host-name', # use `localhost` if you are on local enviournment.
    'user': 'your-user', # mostly user is `root`.
    'password': 'your-database-password',
    'database': 'your-database-name'
}
